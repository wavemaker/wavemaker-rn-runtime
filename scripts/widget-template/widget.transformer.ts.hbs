import { HTMLElement } from 'node-html-parser';

import { Import, transformAttrs, Transformer } from '../../transpile';

export const imports: Import[] = [
  { name: 'Wm{{widget.name.camelcase}}', from: '@wavemaker/app-rn-runtime/components/{{widget.group}}/{{widget.name.hyphenated}}/{{widget.name.hyphenated}}.component'}
];

export default {
  pre: (element: HTMLElement) => `<Wm{{widget.name.camelcase}} ${transformAttrs(element)}>`,
  post: (element: HTMLElement) => '</Wm{{widget.name.camelcase}}>',
  imports: (element: HTMLElement) => imports
} as Transformer;