import React, { ReactNode } from 'react';
import { Text, TouchableOpacity } from 'react-native';
import { shallow } from 'enzyme';
import renderer from 'react-test-renderer';
import Wm{{widget.name.camelcase}} from '@wavemaker/app-rn-runtime/components/{{widget.group}}/{{widget.name.hyphenated}}/{{widget.name.hyphenated}}.component';

describe('Test {{widget.name.camelcase}} component', () => {
    test('Check validity of sample component', () => {
      const tree = renderer.create(<Wm{{widget.name.camelcase}} name="test_{{widget.name.camelcase}}"/>).toJSON();
      expect(tree).toMatchSnapshot();
    });
});